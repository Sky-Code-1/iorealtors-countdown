// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // This tells Next.js to use `next export` and generate static HTML into the `out` directory
};

module.exports = nextConfig;
